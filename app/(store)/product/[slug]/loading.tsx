import ProductDetailSkeleton from "@/components/ProductDetailSkeleton";

/** Shown while a product page loads — mirrors the real two-column layout. */
export default function ProductLoading() {
  return <ProductDetailSkeleton />;
}
